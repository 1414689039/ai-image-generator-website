# 创建项目交接文档 `Ai操作记忆/trae.md`

为了确保下一位 AI 助手能顺利接手项目，我将整理一份详细的项目交接文档，涵盖以下核心内容：

1.  **项目概览**: 技术栈（React/Vite + Hono/Worker）、架构设计（Pages + Workers + D1 + R2）。
2.  **环境配置**: 关键目录结构、配置文件说明 (`wrangler.toml`, `_redirects`)。
3.  **部署指南**:
    *   **前端**: 修正后的构建与部署命令（Cloudflare Pages）。
    *   **后端**: Worker 发布与数据库迁移命令。
    *   **注意事项**: Windows PowerShell 下 `&&` 运算符的兼容性问题提示。
4.  **关键凭证与资源**:
    *   Cloudflare 账号信息（仅邮箱与 Account ID）。
    *   数据库与存储桶 ID。
    *   GitHub 仓库地址与推送流程。
5.  **核心逻辑备忘 (Gotchas)**:
    *   **反向代理**: 前端 `functions` 如何代理图片流量以绕过网络限制。
    *   **双存储桶策略**: R2 亚太区 (APAC) 与旧桶的回退读取逻辑。
    *   **ZX2 API 适配**: 特殊的响应解析逻辑。
6.  **常用命令速查表**: 启动开发环境、运行测试、数据库操作等。

该文档将写入 `d:\项目\ai生图网站\Ai操作记忆\trae.md`，覆盖现有内容（现有内容仅有一条关于分支的笔记，将被整合进新文档）。