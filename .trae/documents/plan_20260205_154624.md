我将为您完善管理员面板功能，并实现联系客服信息的动态配置。

### 后端修改 (Worker)
1.  **新增公共接口**：
    *   `GET /api/config`：公开接口，用于获取系统配置（如联系客服信息）。
2.  **扩展管理员接口 (`worker/src/routes/admin.ts`)**：
    *   `DELETE /users/:id`：删除用户（级联删除相关数据）。
    *   `DELETE /generations/:id`：删除生成记录/下架分享。
    *   `POST /config`：设置系统配置（如联系客服文案）。
    *   `GET /config`：获取所有系统配置（管理员用）。

### 前端修改 (Frontend)
1.  **管理员入口**：
    *   在 `Header.tsx` 的头像下拉菜单中，如果用户是管理员 (`user.isAdmin`)，显示“管理员面板”入口。
2.  **联系客服功能**：
    *   新建 `ContactModal` 组件，从后端获取客服文案并展示。
    *   修改 `Header.tsx` 中的“联系客服”链接，点击后弹出该模态框。
3.  **完善管理员面板 (`Admin.tsx`)**：
    *   **用户管理**：
        *   实现“调整积分”弹窗功能。
        *   增加“删除用户”按钮及确认逻辑。
    *   **生成记录**：
        *   增加“删除”按钮，允许管理员删除违规图片。
    *   **系统配置**：
        *   新增“联系客服信息配置”区域，支持编辑和保存文本。

### 执行步骤
1.  修改后端 Worker 代码，实现所需的 API 接口。
2.  部署后端 Worker。
3.  修改前端代码，实现管理员入口、联系客服弹窗及管理员面板的各项功能。
4.  构建并部署前端 Pages。